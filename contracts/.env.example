# Deployer EOA private key (with USDC on Arc testnet for gas)
PRIVATE_KEY=0x...

# Fee recipient address (receives platform tips fee)
FEE_RECIPIENT=0x...

# Platform fee in basis points (100 = 1%, max 500 = 5%)
FEE_BPS=100

# For ArrowCopyTradeHook (when PoolManager is available on Arc)
POOL_MANAGER=
AGENT_ADDRESS=
