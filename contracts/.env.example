# Deployer EOA private key (same key signs on Arc + Base Sepolia)
PRIVATE_KEY=0x...

# Fee recipient address (receives platform tips fee)
FEE_RECIPIENT=0x...

# Platform fee in basis points (100 = 1%, max 500 = 5%)
FEE_BPS=100

# ArrowCopyTradeHook — deployed on Base Sepolia (where Uniswap v4 PoolManager lives)
# Base Sepolia PoolManager (official Uniswap v4 deployment)
POOL_MANAGER=0x05E73354cFDd6745C338b50BcFDfA3Aa6fA03408
# Agent EOA address (authorized to execute limit orders on-chain)
AGENT_ADDRESS=
